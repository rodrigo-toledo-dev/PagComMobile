import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
  },
  viewHeader: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
  },
  viewConfig: {
    flexDirection: 'row',
  },
  logo: {
    resizeMode: "contain",
    width: 100,
    height: 50,
  },
});
